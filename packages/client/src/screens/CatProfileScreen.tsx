import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

/**
 * TODO: Implement CatProfileScreen
 * - Display cat photo, name, and basic info
 * - Show ownership level and XP progress
 * - Recent sightings timeline
 * - Donate food button
 * - Request medical care button
 * - Join community chat button
 */

export function CatProfileScreen() {
  return (
    <View style={styles.container}>
      <Text>Cat Profile Screen — TODO: Implement cat details view</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
