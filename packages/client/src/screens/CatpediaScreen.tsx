import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

/**
 * TODO: Implement CatpediaScreen
 * - List of cat breeds with images
 * - Search/filter functionality
 * - Tap to see detailed breed info and care guide
 */

export function CatpediaScreen() {
  return (
    <View style={styles.container}>
      <Text>Catpedia Screen — TODO: Implement breed encyclopedia</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
