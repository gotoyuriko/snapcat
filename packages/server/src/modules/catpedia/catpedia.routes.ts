import { Router } from 'express';

/**
 * TODO: Wire up catpedia routes
 * GET /         — Get all catpedia entries (paginated)
 * GET /:breed   — Get info for a specific breed
 */

export const catpediaRoutes = Router();

// catpediaRoutes.get('/', controller.getAll);
// catpediaRoutes.get('/:breed', controller.getByBreed);
