import { Router } from 'express';

/**
 * TODO: Wire up sighting routes
 * POST /          — Report a new cat sighting
 * GET  /area      — Get sightings within a bounding box
 * GET  /cat/:catId — Get sightings for a specific cat
 */

export const sightingRoutes = Router();

// sightingRoutes.post('/', authMiddleware, controller.report);
// sightingRoutes.get('/area', authMiddleware, controller.getInArea);
// sightingRoutes.get('/cat/:catId', authMiddleware, controller.getByCat);
