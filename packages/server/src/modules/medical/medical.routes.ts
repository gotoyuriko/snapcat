import { Router } from 'express';

/**
 * TODO: Wire up medical routes
 * POST /              — Create a medical care request
 * POST /:id/approve   — Approve request (staff/partner)
 * POST /:id/document  — Upload receipt/document
 * POST /:id/complete  — Mark as completed
 */

export const medicalRoutes = Router();

// medicalRoutes.post('/', authMiddleware, controller.create);
// medicalRoutes.post('/:id/approve', authMiddleware, controller.approve);
// medicalRoutes.post('/:id/document', authMiddleware, controller.uploadDocument);
// medicalRoutes.post('/:id/complete', authMiddleware, controller.complete);
