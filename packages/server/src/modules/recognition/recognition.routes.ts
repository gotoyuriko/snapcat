import { Router } from 'express';

/**
 * TODO: Wire up recognition routes
 * POST /recognize — Upload image for cat detection + identification
 */

export const recognitionRoutes = Router();

// recognitionRoutes.post('/recognize', authMiddleware, controller.recognize);
