/**
 * Auth Module
 * Handles user registration, login, JWT token management, and refresh token rotation.
 */

export { AuthService, TokenPair, AccessTokenPayload } from './auth.service';
export { AuthController } from './auth.controller';
export { authRoutes } from './auth.routes';
