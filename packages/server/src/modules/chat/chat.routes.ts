import { Router } from 'express';

/**
 * TODO: Wire up chat routes
 * GET /cat/:catId/messages — Get chat history for a cat
 */

export const chatRoutes = Router();

// chatRoutes.get('/cat/:catId/messages', authMiddleware, controller.getMessages);
